import { styled } from "styled-components";
import theme from "@/app/theme";

export const Header = styled.header`
	/* position: fixed;
	top: 0;
	right: 0;
	left: 0;
	height: auto;
	color: #fff;
	z-index: 1; */
`;
